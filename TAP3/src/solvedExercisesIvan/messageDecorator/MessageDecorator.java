package solvedExercisesIvan.messageDecorator;

// Decorator that will define the method to apply to each call of this decorator.
public interface MessageDecorator {
    public String getText(); // same method as parent
}
